<Alloy>
    <!-- Main Window -->
    <Window id="todoWin" title="Todo">
        <TableView id="todoTable" dataCollection="todo"
        	           dataFilter="whereFunction" dataTransform="transformFunction">
        		<Require src="_albums_feed"/>
        </TableView>

        <!-- iOS: footer with buttons -->
        <View platform="ios" id="footer">
            <TabbedBar onClick="showTasks" id="tabbedbar">
                <Labels>
                    <Label>Facebook</Label>
                    <Label>Twitter</Label>
                    <Label>Others</Label>
                </Labels>
            </TabbedBar>
        </View>

        <!-- Android: menu -->
        <Menu platform="android">
            <MenuItem title="All" onClick="showTasks"/>
            <MenuItem title="Active" onClick="showTasks"/>
            <MenuItem title="Done" onClick="showTasks"/>
        </Menu>
    </Window>

    <!-- Mobileweb -->
   	<Window id="index" platform="mobileweb">
	    <NavigationGroup id="navgroup">
	        <Require src="master" id="master"/>
	    </NavigationGroup>
   	</Window>

  	<!-- Android -->
    <Require id="master" src="master" platform="android"/>


</Alloy>